name: abd_libraries

services:
  library_user:
    container_name: lus
    build:
      context: ./library_user_service
      dockerfile: Dockerfile.dev
    volumes:
      - ./library_user_service:/usr/src/app
    ports:
      - "8081:3000"
